import './bootstrap';
// Import ApexCharts (installed via npm) and expose globally for inline page scripts
import ApexCharts from 'apexcharts';
window.ApexCharts = ApexCharts;

// You can also keep other app bootstrap code here
